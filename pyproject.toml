[project]
name = "axon-booth-backend"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "python-dotenv>=1.0.0",
    "python-multipart>=0.0.9",
    "openai>=1.12.0",
    "elevenlabs>=1.0.0",
]

[tool.vercel]
entrypoint = "booth_backend.api.index:app"
